spring.application.name=ascension

# MySQL connection
spring.datasource.url=jdbc:mysql://localhost:3306/ascension?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=narayan23
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=mySecretKeyThatIsAtLeast32CharactersLongForSecurity
jwt.expiration=86400000

# Jackson Configuration - Fix Hibernate Proxy Serialization
spring.jackson.serialization.fail-on-empty-beans=false
spring.jpa.open-in-view=true